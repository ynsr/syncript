Update `syncript sync` command so `--reset` could be used by any other parameters except below parameters:

```
  --push-only       
  --pull-only        
  --poll-interval N  
  --poll-timeout N  
```
