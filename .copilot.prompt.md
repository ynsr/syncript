Below file and folder patterns in `.stignore` are being ignored during the sync process, while should be excluded from the sync operations to prevent unnecessary files and folders from being transferred or processed.

```
**/target/classes/**
**/target/*.jar
**/target/*.jar*
**/target/**/*.class
```

Update the ignorance logic in the sync engine to ensure that any files or folders matching similar patterns are properly excluded from the sync operations. This will help optimize the sync process by avoiding the transfer of build artifacts and compiled classes that are not needed on the remote server.
