Update the `syncript init` command to include `llm_model` and `socks_proxy` options from user input and save them in the `~/.syncript` file. The `llm_model` option should allow users to specify the default language model for the copilot command, while the `socks_proxy` option should allow users to specify a SOCKS proxy for network connections. 

Also update `install-unix.sh` and `install-windows.ps1` scripts to include prompts for these new options during the installation process. Ensure that the values for `llm_model` and `socks_proxy` are properly saved in the `~/.syncript` file and can be accessed by the `syncript copilot` command when executed.

Default value for `llm_model` should be `gpt-5.3-codex` if the user does not provide any input. The `socks_proxy` option can be left empty if the user does not want to use a proxy but show `socks_proxy: socks5://localhost:10808` as sample SOCKS proxy value.
